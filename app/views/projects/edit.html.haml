
%h1 Editing #{@project.name}

#form= render 'form'

- if @readonly
	= link_to 'Edit', edit_project_path(@project), :class => "btn btn-primary"


= link_to 'Cancel', projects_path, :class => "btn btn-primary"

